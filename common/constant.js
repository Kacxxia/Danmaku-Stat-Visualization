// Communication CMD
const CMD_MSG = "msg"
const CMD_SETTING = "setting"
const CMD_DATA = "data"
const CMD_CONNECT = 'connect'
cosnt CMD_STATS = "stats"

const WS_SERVER_PORT = 8080

module.exports = {
  CMD_MSG,
  CMD_SETTING,
  CMD_DATA,
  CMD_CONNECT
}